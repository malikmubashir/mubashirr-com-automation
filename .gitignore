.env
*.pyc
__pycache__/
*.egg-info/
.venv/
venv/
.DS_Store

# Images ARE committed now — the host-side cron-pull PHP reads them from
# the local git checkout (cPanel Git Version Control) and sideloads them
# into WP media library. Direct REST upload from GH Actions is blocked
# by the host's origin firewall (silent SYN drop on Azure egress).
# drafts/*/images/*.png

# Local backups
*.sql
*.sql.age
